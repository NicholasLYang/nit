export interface Issue {
  id: string;
  number: number;
  titleHTML: string;
  bodyHTML: string;
}

export interface PullRequest {
  id: string;
  number: number;
  titleHTML: string;
  bodyHTML: string;
}
